// Barrel for icon exports — resolves via the `@icons` alias.
export { BookOpenIcon, HeartIcon, HomeIcon, ChurchIcon, ROOM_ICONS } from "./room-icons";
export type { RoomIconName } from "./room-icons";
